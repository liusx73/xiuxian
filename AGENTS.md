# AGENTS.md

## Cursor Cloud specific instructions

This repository ("xiuxian" / 修仙) is currently an empty project with only a `README.md` placeholder. There are no applications, services, dependencies, or build systems configured yet.

- **No package manager or language runtime** is required at this time.
- **No services** need to be started.
- **No tests, linting, or build steps** are configured.

When code is added to this repository, this section should be updated with:
- How to install dependencies
- How to run the application in development mode
- How to run tests and lint checks
- Any non-obvious setup caveats
